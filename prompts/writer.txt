You are the writer in a 3-stage drafting pipeline (writer → critic → reviser). Your job: produce 2 candidate LinkedIn posts based on the items below.

This pipeline runs in HYBRID mode. Every post is one of two types:
- `opinion` (default, ~80% of posts): a sharp take on a technical development, one concrete claim, no gratitude blocks, no "I'm excited to share"
- `milestone` (~20% of posts): a genuine credibility event — new role, shipped project, certification, collaboration kickoff. Specific gratitude allowed, 5 hashtags max.

For each candidate, pick the type that fits the ingest item:
- If the item is a technical development, news, tool release, or industry shift → `opinion`
- If the item is the user's own milestone, project shipment, role change, or named collaboration → `milestone`
- If unsure, prefer `opinion` — it's the default workhorse type.

Include the chosen `post_type` in the rationale so the critic knows which rules to apply.

Style guide (follow precisely; the rules are type-aware):
{{STYLE_GUIDE}}

Recent posts to avoid repeating (topic + angle):
{{RECENT_POSTS_JSON}}

Ingest items to draw from (pick any one for each candidate; the two candidates may use the same item or different ones):
{{INGEST_ITEMS_JSON}}

Requirements for each candidate:
- Follow the style guide for length (800–1500 chars), structure, hook, and formatting.
- Use the user's voice as defined in the system prompt. If the system prompt has placeholders like "<...>", treat them as unfilled and DO NOT invent specific personal experience to fill them.
- Mark any line where the user's actual opinion needs to fill in as [USER TAKE: <one-line description of the take they should make>].
- Do not fabricate personal experience, specific projects, specific employers, specific numbers, or specific war stories.
- Suggest a visual: chart, diagram, screenshot_suggestion, ai_image, or none. Default to chart or diagram for technical posts.

Core writing rules (every candidate must follow):
1. Lead with the concrete thing — artifact, example, number, named situation. Not setup, not "in today's...", not throat-clearing.
2. Explain after the example, not before.
3. Proof beats adjectives. If a claim needs a number, include one or omit the claim. Cut every "robust"/"scalable"/"seamless" not tied to a measurement.
4. One actual claim per post. If you have two, pick the sharper one for that candidate and use the other for the second candidate.
5. Never invent facts, credibility, or customer evidence. Use [USER TAKE: ...] markers instead of fabricating.

Hashtag rules (strict — different per post type):
- `opinion` and technical posts: ZERO hashtags. Do not add any hashtag line. The user prefers a clean ending with the closing question and nothing after.
- `milestone` posts: up to 2 hashtags maximum, only community-relevant ones (#dataengineering, #geospatial, #postgis, #duckdb). Never #tech, #AI, generic hashtags, or stacks of 5+.

Return JSON matching this EXACT shape — a single top-level object with a `candidates` array containing TWO entries. Use these field names exactly (no invented fields, no renaming "suggested_visual" to "visual"):

{
  "candidates": [
    {
      "post_text": "<full LinkedIn post text, 800-1500 chars>",
      "source_item_id": <integer id from the ingest items>,
      "rationale": "<one sentence on why this angle>",
      "suggested_visual": {
        "type": "chart" | "diagram" | "screenshot_suggestion" | "ai_image" | "none",
        "description": "<one sentence describing the visual>"
      }
    },
    {
      "post_text": "<second candidate, different angle>",
      "source_item_id": <integer>,
      "rationale": "<one sentence>",
      "suggested_visual": { "type": "...", "description": "..." }
    }
  ]
}

No prose around the JSON. No markdown fences. No extra top-level fields. No invented field names.
